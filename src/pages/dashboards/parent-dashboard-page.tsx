import { FoundationPage } from '../../components/common/foundation-page'

export function ParentDashboardPage() {
  return (
    <FoundationPage
      eyebrow="Dashboard"
      title="Parent Dashboard"
      description="Placeholder dashboard for guardians to track registered children, schedules, and reminders."
      status="Route ready"
    />
  )
}
